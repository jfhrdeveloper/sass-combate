# Tareas pendientes y bitácora

> Control de estado, decisiones y trabajo pendiente. Una entrada por sesión de trabajo.
> Fechas en formato ISO año-mes-día (`YYYY-MM-DD`).

## Roadmap
- [x] Integrar pasarela de pago con tarjeta — Culqi conectado (`src/services/pagos/culqi.ts`), exclusivo para que la academia contrate sass-combate (plan Academia u evento puntual). **La inscripción del peleador nunca pasa por Culqi** — ver bitácora de 2026-08-06. **Sin validar contra una cuenta Culqi real todavía**.
- [x] Notificaciones al peleador cuando su pelea se acerca — email (Resend), SMS y WhatsApp (Twilio), push web (VAPID); ver `src/lib/notificaciones/`. **Sin credenciales reales de ningún proveedor todavía**, cada canal queda inactivo hasta que se configure.
- [ ] Reemplazar los iconos PWA placeholder por el arte de marca real (192px y 512px).
- [ ] Conectar el proyecto a Supabase real y verificar aislamiento entre organizaciones (crear dos academias de prueba y confirmar que ninguna ve datos de la otra) antes de exponer a producción. Ninguna migración (`20260101000001` a `20260101000008`) se corrió todavía contra un proyecto real; el código ya está preparado para ese momento (ver bitácora de 2026-07-29), falta solo crear el proyecto y correrlas.
- [ ] Desplegar a Vercel (o el hosting elegido) y configurar dominio con soporte de subdominios por academia.

## Pendientes activos
- [ ] **Auditoría grande de interfaz/backend pedida el 2026-08-06** (ver esa entrada de bitácora): el usuario pidió implementar las 7 fases de una vez. Fases 1, 2 y 3 cerradas (ver bitácora de hoy). Quedan las fases 4 a 7 (paginación 4 mobile/8 desktop, CRUD completo de categorías/equipo/atletas, backend robusto para 300 espectadores, animaciones + skeleton).
- [x] Extender la inscripción desde el panel para pasar por la cola offline (hoy va directo contra el servidor; solo resultados, pesaje y asistencia usan la cola).
- [x] Formalizar tokens de color para estados (éxito/aviso/error/info) en `tailwind.config.ts` — hoy se usan utilidades sueltas.
- [x] Conectar `/p/[token]` y `/e/[org]/[evento]` a datos reales de Supabase — ver bitácora de hoy (`src/lib/publico.ts`).
- [x] Vincular `pago_inscripcion` para que aprobar un pago marque inscripciones como pagadas vía el trigger `aplicar_pago` — ver bitácora de hoy (`vincular_pago_inscripciones`).
- [x] `src/app/app/mi-club/page.tsx` y `src/app/app/eventos/[id]/page.tsx` ya usan `src/lib/consultas.ts` en vez de `INSCRIPCIONES_DEMO`/`PELEAS_DEMO` fijos — ver bitácora de hoy.
- [x] El monto que cobra "Mi club" ya usa `precio_inscripcion()` (vía `obtenerPrecioInscripcion`) en vez de `alumnos × S/50` fijo — ver bitácora de hoy.
- [x] Historial de compras de plan (Academia y por evento) — nueva tabla `compra_plan`, visible en `/app/plan` — ver bitácora de hoy.
- [ ] Falta el domicilio de sass-combate en `/libro-de-reclamaciones` (campo legalmente esperado del proveedor) — el usuario confirmó que todavía no tiene dirección física; completarlo ahí cuando esté disponible. El RUC (10708343931) ya se agregó, confirmado directamente por el usuario en el chat el 2026-07-29 (no la notificación sospechosa de la entrada anterior — ver nota de seguridad ahí).
- [ ] Cobro recurrente real del plan Academia (webhooks + tokens de tarjeta guardados en Culqi). Hoy `/app/plan` cobra una sola vez por período y no se renueva sola — alcance decidido a propósito en la sesión de 2026-07-29, ver esa entrada.
- [ ] `src/app/app/eventos/[id]/emparejar/page.tsx` y `.../pesaje/page.tsx` siguen sembrados con `INSCRIPCIONES_DEMO` (son Client Components; pesaje además lo usa a propósito como semilla de su caché offline). No estaban en el pendiente que se cerró hoy — conectarlos a datos reales necesitaría un wrapper de servidor o el cliente Supabase de navegador (`src/lib/supabase/client.ts`), que hoy no se usa en ninguna página.
- [ ] **No existe ninguna UI para crear/editar áreas de un evento** (ring/tatami/jaula, con sus modalidades y hora de inicio) — hoy `AREAS_DEMO` es el único ejemplo que existe; en producción real habría que insertarlas a mano en Supabase. Descubierto el 2026-08-04 al ampliar el catálogo de modalidades (ver esa entrada); no se construyó, es alcance separado y bastante grande (una pantalla de gestión completa).
- [ ] `mesa/[eventoId]/page.tsx` sigue usando `CATEGORIAS_DEMO`/`PELEAS_DEMO`/`AREAS_DEMO` fijos (mismo gap de siempre, ver el pendiente de arriba) — el chip de categoría de peso que se agregó el 2026-08-04 hereda esa misma limitación: en real necesitaría `listarCategorias` server-side igual que `eventos/[id]/page.tsx`.
- [x] Descuento al aprobar un pago de inscripción — ver bitácora de hoy (`pago.descuento_tipo`/`descuento_valor`, `RevisarPago`).

### 2026-08-06 (5) — Auditoría grande: Fase 3 (badges, grids, tablas, Poppins)
- **Qué cambió:**
  - **Badges unificados bajo `Insignia`:** `mi-club/page.tsx` (chips "por pagar"/"pagado" armados a mano con `py-1`) y `pagos/page.tsx` (un `COLOR` map duplicado casi idéntico al de `Insignia`) pasaron a usar `<Insignia estado={...} />`. `badge.tsx` ganó 5 claves nuevas: `en_revision`/`aprobado`/`rechazado` (estado de pago) y `por_pagar`/`pagado` (estado de inscripción visto desde Mi Club, con su propio color de aviso en vez de reusar el `pendiente` neutro que ya significaba otra cosa). De paso, `Tarjeta` (`card.tsx`) ganó `h-full` para que las tarjetas de un mismo grid queden a la misma altura si su contenido varía en largo.
  - **4 grids que saltaban de 1 a 4 columnas** (`admin/page.tsx`, `atletas/[id]/page.tsx`, `emparejar/page.tsx`, `eventos/[id]/page.tsx`) pasaron a `grid-cols-2 sm:grid-cols-4` (2 columnas en mobile, como pidió el usuario), copiando el patrón que `mesa/[eventoId]/page.tsx` ya tenía bien.
  - **3 tablas con `overflow-hidden` que recortaban contenido en mobile** (`admin/page.tsx`, `eventos/[id]/page.tsx`, `mi-club/cargar.tsx`) pasaron a `overflow-x-auto` con `min-w-[...]` explícito en la `<table>`, para que el scroll quede contenido en el wrapper (nunca en el body de la página) en vez de aplastar las columnas.
  - **Tipografía a Poppins:** reemplaza Barlow Condensed + Inter en `layout.tsx`. Se mantuvo el mismo esquema de dos roles ya existente (`--font-display`/`--font-body`, usados en todo el proyecto vía `font-display`/`font-body` de Tailwind) para no tocar ningún componente: ahora ambos roles cargan Poppins, con grosores distintos (`display` 500/600/700, `body` 400/500/600) marcando la jerarquía en vez de dos tipografías separadas. Poppins no es una fuente variable en Google Fonts (a diferencia de Inter, que se cargaba sin lista de grosores), así que se listaron solo los grosores que el proyecto ya usa (confirmado con grep de `font-(normal|medium|semibold)` en todo `src/`, no hay ningún `font-bold` ni más pesado en ningún lado hoy).
  - Documentado en `style-guide.md`: tipografía, regla de grids (nunca saltar de 1 a más de 2 columnas), regla de tablas anchas (`overflow-x-auto`, nunca `overflow-hidden`), y los estados nuevos de `Insignia`.
  - `npm run typecheck`, `npm run lint`, `npm test` (60 pruebas) y `npm run build` pasan (Poppins se descarga y compila bien en el build). Verificado que la respuesta del servidor de desarrollo referencia los archivos de Poppins compilados.
- **Por qué:** pedido explícito del usuario, Fase 3 del plan de 7.
- **Pendiente / riesgos conocidos:**
  - No se verificó visualmente en un navegador real cómo se ve Poppins junto a los números `tabular-nums` de los marcadores (Barlow Condensed era más angosta) — vale la pena una revisión visual cuando haya oportunidad, aunque no bloquea nada funcionalmente.
  - Siguen las fases 4 a 7.

### 2026-08-06 (4) — Auditoría grande: Fase 2 (Select/Dialog propios, sin flechas nativas)
- **Qué cambió:** el usuario pidió implementar las 7 fases del plan de una vez, sin esperar confirmación fase por fase. Para el punto pendiente (alcance de shadcn), se siguió la interpretación propuesta y no corregida por el usuario: shadcn conviviendo con el sistema propio, solo para lo que faltaba.
  - **`Select` y `Dialog` propios** (`src/components/ui/select.tsx`, `dialog.tsx`): envoltorio sobre `@radix-ui/react-select` y `@radix-ui/react-dialog` (paquetes nuevos, sin correr `shadcn init` a propósito, que hubiera reescrito `globals.css`/`tailwind.config.ts` con el theme por defecto de shadcn y pisado los tokens de color ya definidos). Usan `cn()` (`@/utils/cn`, ya idéntico al de shadcn) y los tokens propios (`border-borde`, `bg-panel`, etc.), no el theme zinc/oklch por defecto. Animación de `Dialog` con keyframes propios en `tailwind.config.ts` (`overlay-in/out`, `dialog-in/out`) en vez del plugin `tailwindcss-animate`, mismo criterio de no sumar una dependencia por 2 animaciones — con soporte de `prefers-reduced-motion` en `globals.css`.
  - **Los 8 `<select>` nativos reemplazados** por `Select`: `atletas/[id]/agregar.tsx`, `equipo/page.tsx` (que además tenía su propio problema de rol crudo sin `NOMBRE_ROL`, corregido de paso), `categorias-evento.tsx` (2), `mi-club/comprobante.tsx`, `pagos/revisar.tsx`, `libro-de-reclamaciones/formulario.tsx`, `selector-rol-demo.tsx` (el más particular: se auto-enviaba al cambiar con `form.requestSubmit()`, ahora usa `onValueChange` + un `ref` al formulario). Donde `value=""` representaba una opción real ("Cualquiera" en sexo de categorías), se usó un valor centinela no vacío (`"todos"`) porque Radix no permite string vacío en `SelectItem`; el server action (`actions/eventos.ts::crearCategoria`) lo traduce a `undefined`.
  - **Flechas de `type="number"` ocultas globalmente**: 2 líneas en `globals.css` (`::-webkit-inner-spin-button`/`-moz-appearance: textfield`), sin tocar cada input uno por uno — cubre los 4 casos encontrados en la auditoría automáticamente.
  - Documentado como regla en `style-guide.md` (`Componentes UI` y `Reglas generales`): nunca un `<select>` nativo, la flecha del `Select` propio siempre con espacio propio (`pr-9`/`right-3`, nunca pegada al borde).
  - `npm run typecheck`, `npm run lint`, `npm test` (60 pruebas) y `npm run build` pasan. Verificado con `curl` contra el servidor de desarrollo: cero `<select>` visibles en el HTML (el único `<select>` que aparece es el que Radix genera oculto —`aria-hidden`, clip a 1px— para que el `FormData` del formulario nativo lo siga leyendo), el `SelectTrigger` renderiza con el padding correcto.
- **Por qué:** pedido explícito del usuario, Fase 2 del plan de 7 aprobado en la sesión anterior.
- **Pendiente / riesgos conocidos:**
  - No se probó la interacción real (abrir el desplegable, navegar con teclado) en un navegador de verdad — solo se verificó el HTML servido y que Radix es la implementación de referencia de estos patrones de accesibilidad, no una construida a mano.
  - Siguen las fases 3 a 7.

### 2026-08-06 (3) — Auditoría grande de interfaz/backend: Fase 1 (texto y datos crudos)
- **Qué cambió:** el usuario pidió una auditoría completa de la interfaz (alineación de cards/botones/badges, grids en mobile, selects propios, inputs numéricos sin flechas, scroll horizontal, paginación 4 mobile/8 desktop, CRUD completo, guiones largos "—" y mayúsculas, campos crudos tipo `variable_id`, tipografía Poppins) y del backend (robustez para ~300 espectadores en vivo, pesaje sin lentitud), además de qué rescatar de sass-optica. Se lanzaron 4 investigaciones en paralelo (consistencia UI, texto/campos crudos, comparación con sass-optica, rendimiento backend) y se armó un reporte detallado (artifact) con 20 hallazgos priorizados, comparación con sass-optica y un plan de 7 fases, antes de tocar código (pedido explícito del usuario: "dame todo detallado antes de empezar").
  - **Decisiones del usuario sobre el plan:** (1) paginación 4 en mobile / 8 en desktop, mecanismo elegido por el asistente al no especificar: cookie de ancho de pantalla, mantiene el resto server-side; (2) skeleton con `boneyard-js` (existe en npm, v1.9.0, pero declara `playwright` como dependencia, hay que pilotearlo primero en pesaje y medir el impacto real en el build/tamaño de función serverless antes de expandirlo); (3) shadcn y el sistema propio (`Boton`/`Tarjeta`/`Insignia`) conviven, sin migrar lo existente (interpretación del asistente de un mensaje cortado del usuario, **pendiente de confirmación explícita**); (4) cambio a Poppins confirmado, sin necesitar aprobación adicional.
  - **Fase 1 implementada hoy** (texto y datos crudos, no depende de ninguna decisión pendiente):
    - **Mapa de roles legible:** `NOMBRE_ROL: Record<Rol, string>` nuevo en `src/config/roles.ts` (`dueno → "Dueño"`, etc.). Reemplaza el rol crudo en los 4 sitios donde se mostraba sin traducir: `entrar/page.tsx` (botones de acceso directo y lista de credenciales demo), `selector-rol-demo.tsx` (el `<select>` de cambio de rol en sidebar/bottom-nav), `sidebar-app.tsx` (abreviatura de 3 letras cuando el sidebar está colapsado). De paso, `equipo/page.tsx` tenía su propio array `ROLES` local con el mismo problema (`{valor} — {desc}` mostraba "admin" crudo) — también corregido.
    - **Guiones largos "—" eliminados de todo texto visible al usuario** (no se tocaron los comentarios de código, que son el estilo de escritura del equipo): placeholders de "sin dato" pasaron a un guion corto simple `"-"` (`kg()` en `utils/format.ts`, usado en toda la app; `tarjeta-pelea.tsx`; varias tablas), separadores dentro de frases se reescribieron con coma o dos puntos en vez de guion largo (`entrar`, `auditoria`, `plan`, `mesa`, `desbloquear`, `emparejar`, `privacidad`). El título de **todas las páginas del sitio** usaba la plantilla `"%s — sass-combate"` en `layout.tsx`, ahora `"%s · sass-combate"`. De paso se encontró y corrigió un bug real independiente: 3 páginas (`libro-de-reclamaciones`, `terminos`, `privacidad`) hardcodeaban el sufijo completo en su `title`, que Next.js volvía a pasar por la plantilla del layout, duplicando la marca (`"Libro de Reclamaciones — sass-combate · sass-combate"`, verificado con `curl` antes y después del fix). También se corrigieron los guiones largos en el generador de actas/credenciales (`api/eventos/[id]/acta`, `api/eventos/[id]/credenciales`), que no habían aparecido en el primer barrido por ser rutas API, no páginas.
    - **Auditoría ya no muestra campos crudos:** nueva migración `20260101000015_auditoria_con_nombre.sql` (política `perfil_companeros`, que deja leer el perfil de cualquiera que comparta organización vía `miembro`, sumada a `perfil_propio` que sigue intacta; vista `v_auditoria` con el join explícito a `perfil` porque `auditoria.usuario_id` referencia `auth.users`, no `perfil`, directamente, así que PostgREST no lo puede embeber solo). `services/auditoria.ts` gana `nombreUsuarioAuditoria()` (nombre o correo del perfil, "Sistema" si no hay) y `nombreCampoAuditoria()` (mapa de las columnas de `resultado` más comunes a texto legible: `ganador_id → "Ganador"`, etc.). `auditoria/page.tsx` ya no muestra el UUID de `usuario_id` ni el nombre crudo de columna.
    - Capitalización: único caso encontrado en el barrido (`atletas/[id]/agregar.tsx`, placeholder "decisión, rsc…") corregido a "Decisión, RSC, etc.".
  - `npm run typecheck`, `npm run lint`, `npm test` (60 pruebas) y `npm run build` pasan. Verificado con `curl` contra el servidor de desarrollo en modo demo: `/entrar` muestra "Dueño"/"Administrador"/etc. con tilde y mayúscula correctas, `/app/auditoria` muestra "Ganador"/"Método"/"Mesa de control (demo)" en vez de los campos crudos (se confirmó que un "ganador_id" que seguía apareciendo en la respuesta cruda era la `key` interna de React del payload RSC, no texto renderizado al usuario, no un bug).
- **Por qué:** pedido explícito del usuario, primera fase de un plan de 7 que él mismo aprobó (con la reserva del punto 3, todavía sin confirmar del todo).
- **Pendiente / riesgos conocidos:**
  - Nada de la migración `20260101000015` se probó contra un Supabase real (mismo riesgo de siempre, sin proyecto real todavía).
  - `ganador_id`/`azul_id` etc. dentro del `antes`/`despues` de auditoría siguen mostrando el ID crudo de la inscripción (no su nombre) como *valor* del campo — se humanizó el *nombre* de la columna, no el valor; resolver eso necesitaría cruzar cada entrada de auditoría contra `inscripcion`/`peleador` en el momento de listar, no se hizo hoy porque no era parte del pedido original (quedó anotado como posible mejora futura, no como bug).
  - Faltan las fases 2 a 7 del plan grande (ver Pendientes activos arriba).

### 2026-08-06 (2) — Descuento al aprobar un pago de inscripción
- **Qué cambió:** siguiendo la aclaración de la entrada anterior (el cobro de inscripción es manual, con descuentos que decide cada academia), el usuario pidió agregar el descuento como parte del flujo de revisión: un botón en `/app/pagos` para que dueño/admin lo agregue **al aprobar**, no el coach al registrar el comprobante. Diseño confirmado con el usuario antes de construir: el descuento vive solo en la pantalla de revisión (no en Mi Club), acepta monto fijo en soles o porcentaje según el caso, y es puntual por pago (no una configuración guardada por club — se decidió empezar simple).
  - **Schema:** migración `20260101000014_descuento_pago.sql` — `pago.descuento_tipo` (`'monto'|'porcentaje'`), `pago.descuento_valor` (numeric, con constraint de que ambos campos van juntos y que un porcentaje no pase de 100), `pago.monto_final` (columna generada, el monto ya con el descuento aplicado, nunca baja de 0). No hizo falta tocar RLS: la política `pago_revision` ya dejaba a dueño/admin actualizar cualquier columna de la fila.
  - **Lógica pura:** `src/lib/descuentos.ts::aplicarDescuento()` (con `tests/descuentos.test.ts`, 6 casos) replica la misma fórmula de la columna generada, para el cálculo en el cliente (vista previa antes de aprobar) y en `resumenIngresos7Dias`/`montoFinal()` (`src/services/pagos.ts`) — el gráfico de ingresos y "Recaudado" en `/app/pagos` ahora suman el monto neto, no el bruto.
  - **UI:** `pagos/revisar.tsx` (`RevisarPago`) gana un botón "Agregar descuento" que despliega un selector (S/ monto fijo | % porcentaje) + input de valor + vista previa ("Total original S/ X → a cobrar S/ Y") antes de aprobar. `pagos/page.tsx` muestra el precio tachado + el neto y la etiqueta del descuento en cada pago ya aprobado con descuento. `actions/pagos.ts::revisarPago` valida con Zod (porcentaje ≤ 100) y solo persiste `descuento_tipo`/`descuento_valor` si de verdad se activó el toggle (si no, quedan `null`, sin pisar un descuento existente por accidente).
  - Datos demo (`services/pagos.ts`): 2 de los pagos ya aprobados llevan un descuento de ejemplo (Grinta Fight 10%, Dojo Boyka Fight S/ 15) para poder ver el tachado/etiqueta sin Supabase real.
  - `npm run typecheck`, `npm run lint` y `npm test` (60 pruebas, 6 nuevas) pasan. Verificado con `curl` contra el servidor de desarrollo en modo demo: el botón "Agregar descuento" aparece en los 4 pagos `en_revision` visibles para dueño/admin, y los pagos demo con descuento muestran el tachado y el neto correcto (S/ 450 → S/ 405, 10%).
- **Por qué:** pedido explícito del usuario, continuación directa de la corrección de alcance de Culqi de la entrada anterior.
- **Pendiente / riesgos conocidos:**
  - No se probó contra un Supabase real (columna generada `monto_final` y los constraints están razonados contra el schema, no verificados de punta a punta).
  - Sigue sin existir un descuento "por club" reutilizable (se decidió a propósito empezar solo por pago) — si más adelante hace falta, es la migración de `20260101000014` la que habría que extender.
  - El descuento no se puede aplicar en modo demo de punta a punta (la acción corta antes por `!HAY_SUPABASE`), igual que el resto de acciones de escritura en modo demo — la vista previa del lado del cliente sí funciona sin Supabase.

### 2026-08-06 — Corrección de alcance: Culqi es solo para contratar sass-combate, nunca para la inscripción del peleador
- **Qué cambió:** el usuario aclaró que el cobro de inscripción a academias/competidores es **manual por decisión de negocio** (cada academia lo cobra a su manera — efectivo, Yape, transferencia — y aplica descuentos propios; quieren control total, no una pasarela automática). El pago con tarjeta de Culqi que se integró en sesiones anteriores era para otra cosa: que la academia contrate el servicio SaaS (plan Academia o desbloqueo de evento). Una sesión anterior (2026-07-28) había conectado Culqi también al flujo de inscripción (`mi-club/pago-tarjeta.tsx` + `/api/pagos/culqi`), lo cual contradice el modelo real y se corrigió:
  - **Eliminado:** `src/app/app/mi-club/pago-tarjeta.tsx` (widget Checkout.js para inscripción) y `src/app/api/pagos/culqi/route.ts` (endpoint que cobraba y auto-aprobaba el pago de inscripción). `mi-club/comprobante.tsx` perdió la opción "Tarjeta" del selector de método — ahora solo Yape/Plin/transferencia/efectivo, todos por comprobante manual revisado por el organizador. El enum de `metodo` en `actions/pagos.ts::registrarPago` también perdió `"tarjeta"` (el check constraint de la tabla `pago` en Postgres se dejó igual a propósito: sigue permitiendo el valor a nivel de esquema, no se tocó una migración ya escrita en una sesión pasada por el mismo criterio que otras entradas de esta bitácora — solo se editan migraciones del mismo día que nunca corrieron contra un proyecto real).
  - **Se mantiene intacto:** Culqi para el plan Academia (`plan/selector-plan.tsx` + `/api/pagos/plan`) y para desbloquear un evento puntual (`eventos/[id]/desbloquear.tsx` + `/api/pagos/evento`) — ambos sobre `src/services/pagos/culqi.ts`, que no se tocó. `vincular_pago_inscripciones` sigue existiendo porque `registrarPago` (el flujo manual) también la usa para enlazar el comprobante aprobado a las inscripciones pendientes del club.
  - Documentación actualizada: `docs/architecture.md` (tabla de integraciones §6, variables de entorno §7, invariantes §10 — nueva regla explícita: "el cobro de la inscripción nunca pasa por una pasarela").
  - `npm run typecheck`, `npm run lint` y `npm test` (54 pruebas) pasan.
- **Por qué:** el usuario notó que el diseño no reflejaba su modelo de negocio real y lo corrigió antes de que llegara a producción.
- **Pendiente:** no existe todavía ninguna función de descuentos por alumno/club al calcular `precio_inscripcion()` — el usuario mencionó "hay descuentos etc" al aclarar el modelo, pero no se pidió implementarlo esta sesión; es un hueco nuevo a definir con el usuario (¿descuento fijo, por cantidad, por club?) antes de construirlo.

### 2026-08-04 (3) — Datos demo a gran volumen, para revisar el diseño bajo carga
- **Qué cambió:** el usuario pidió ver cómo se comporta la interfaz con mucha más información — 15 eventos, muchos peleadores/pesajes/pagos — para revisar el diseño bajo volumen real. Se amplió el dataset demo (decisión explícita del usuario: **dejarlo así de forma permanente, hasta que se suban datos reales a Supabase**, no es un dataset de un solo uso):
  - `EVENTOS_DEMO` (15 eventos, `src/lib/datos.ts`): el primero sigue siendo `EVENTO_DEMO` (id `"demo"`, con todo su detalle ya cargado); los otros 14 solo tienen metadata propia (nombre/fecha/sede/estado variados) — abrir cualquiera de ellos muestra el mismo detalle del evento "demo", porque el modo demo modela un solo evento con contenido, no 15 independientes. `listarEventos`/`obtenerEvento` en `consultas.ts` ahora leen de esta lista.
  - **120 peleadores generados** (además de los 24 ya curados a mano) con un generador determinista (PRNG `mulberry32` con semilla fija, no `Math.random()`, para que las capturas sean reproducibles) — 10 por cada una de las 12 modalidades, con nombre/club/peso/edad/sexo variados. Se emparejan por peso dentro de su propia modalidad para generar ~60 peleas más, reutilizando las mismas 3 áreas (`ring`/`tatami-1`/`jaula`).
  - **35 pagos generados** (además de los 9 curados) con el mismo criterio, para paginación y el gráfico de ingresos de 7 días.
  - Verificado con Playwright: dashboard pagina a 8 (`Página 1 de 2`), pagos pagina a 8 (`Página 1 de 6`), mi-club pagina a 8 (`Página 1 de 18`), mesa sigue capeando en "próximas 8" sin importar el total. **Hallazgo real, no arreglado todavía:** la tabla de peleas de `/app/eventos/[id]` (una por área) NO está paginada — con 72 peleas totales en el evento demo, esa página queda con tablas muy largas (30+ filas en Tatami 1, 36+ en Ring) en una sola vista sin cortar. Puede ser intencional (el organizador quiere ver el día completo de un vistazo) o puede necesitar paginar/colapsar por área — queda para decidir con el usuario, no se tocó.
  - Otro hallazgo: con 144 peleadores repartidos parejo entre 12 disciplinas (~12 cada una), el emparejador deja 80 "sin rival" — la mayoría de los rechazos son "modalidad incompatible" (8891 de ~10203), simplemente porque solo pueden cruzar entre sí los de la misma disciplina. Es el comportamiento esperado del diseño (no un bug), pero confirma que la profundidad por disciplina importa más que la variedad para la tasa de emparejamiento.
- **Por qué:** revisión de diseño bajo volumen pedida explícitamente por el usuario.
- **Pendiente:** decidir si la tabla de peleas de `eventos/[id]` necesita paginar/colapsar por área a este volumen (ver hallazgo arriba). El registro de atletas (`services/atletas.ts`, la lista compartida con historial de peleas, distinta de las inscripciones de un evento) NO se amplió — sigue con su puñado de siempre, fuera del alcance de este pedido.

### 2026-08-04 (2) — Modalidades reales, métodos por disciplina y categorías de peso con nombre
- **Qué cambió:** el usuario preguntó si cada academia podía elegir sus propios métodos de resultado (KO/TKO/sumisión, no solo el vocabulario de kickboxing) y definir categorías de peso con nombre (p. ej. "Peso pluma" en boxeo) con un rango o un peso exacto. Al investigar, la base de datos ya traía esto desde el primer commit (`modalidad.organizacion_id`/`disciplina`/`config`, `categoria.peso_min`/`peso_max`, migración `20260101000001` + catálogo seedeado en `20260101000004`) — el límite era la capa de aplicación, que solo conocía 5 de las 12 modalidades ya sembradas y un único array fijo de métodos. Se resolvió como una etiqueta visual explícita: **las categorías de peso NO cambian el emparejador** (sigue cruzando por `maxDifPesoPct`, decisión confirmada con el usuario antes de tocar código — ver §10 de `architecture.md`), evitando meterse con el motor de emparejamiento y sus pruebas.
  1. **Catálogo ampliado** (`src/types/index.ts`): `ModalidadCodigo` pasó de 5 a 12 códigos (agregó `full_contact`, `boxeo`, `muay_thai`, `mma`, `sanda`, `gi`, `no_gi`, ya sembrados en la base pero ignorados por el código). `DISCIPLINA_POR_MODALIDAD` y `METODOS_POR_DISCIPLINA` nuevos: cada disciplina tiene su propio vocabulario de victoria (kickboxing WAKO sigue en decisión/RSC/abandono/descalificación/walkover; boxeo y muay thai suman KO/TKO; MMA suma sumisión y "no contest"; sanda suma la salida del área; jiu-jitsu (gi/no-gi) es sumisión/puntos/ventaja, sin decisión de jueces). Migración `20260101000013_metodos_por_modalidad.sql` refleja lo mismo en el `config` jsonb de cada modalidad del catálogo global, para cuando haya Supabase real.
  2. **Mesa de control** (`mesa/[eventoId]/page.tsx`): el paso "¿por qué método?" ahora resuelve la modalidad compartida por roja/azul y muestra los métodos de esa disciplina en vez del array fijo de 5.
  3. **Categorías de peso** (`CategoriaPeso` en `types/index.ts`, `src/lib/categorias.ts::categoriaDePeso` con pruebas): función pura que matchea peso+sexo contra una lista de categorías `{nombre, peso_min, peso_max, sexo, modalidad}` — un peso exacto es `peso_min === peso_max`. Nueva sección "Categorías de peso" en `/app/eventos/[id]` (`categorias-evento.tsx`) para crear/eliminar categorías por evento (`crearCategoria`/`eliminarCategoria` en `actions/eventos.ts`, sobre la tabla `categoria` que ya tenía RLS completa desde el primer commit — no hizo falta migración de permisos). El chip de categoría (ej. "Peso pluma") se agregó junto al peso en `eventos/[id]/page.tsx`, `mesa/[eventoId]/page.tsx` y `emparejar/page.tsx`.
  4. **Datos demo ampliados**: 2 peleadores de boxeo (Renzo/Kevin, ~57kg) y 2 de MMA (Marco/Ruben), nueva área "Jaula", y `CATEGORIAS_DEMO` con 5 categorías de ejemplo (Peso pluma/welter de boxeo, -57/-63/-71kg de low_kick) — la pelea de boxeo se puso primera en el orden del Ring a propósito, para que aparezca dentro de las "próximas 8" de la mesa sin tener que resolver antes las de kickboxing.
  5. `npm run typecheck`, `npm run lint`, `npm test` (54 pruebas, 8 nuevas de `categorias.test.ts`) y `npm run build` pasan. Verificado con Playwright contra el servidor de desarrollo: mesa muestra KO/TKO en la pelea de boxeo, la sección de categorías crea/lista bien en claro y oscuro, y el emparejador muestra "(Peso pluma)"/"(-57kg)" junto al peso en los cruces.
- **Por qué:** pedido explícito del usuario tras notar que el sistema no reflejaba esto ("no veo dónde se puede registrar el peso por categoría").
- **Decisión de alcance (confirmada con el usuario antes de empezar):** las categorías son solo una etiqueta, no reemplazan el matching dinámico por tolerancia de peso — la alternativa (categoría rígida que reemplaza el emparejador, o categorías solo para llaves de eliminación) se descartó explícitamente.
- **Pendiente / riesgos conocidos:** no existe UI para crear áreas (ver Pendientes activos arriba) — las categorías se definen por evento pero las áreas de ese evento siguen siendo un hueco. `mesa`/`pesaje`/`emparejar` siguen sin conectarse a Supabase real (gap preexistente, no introducido hoy). Ninguna migración corrió contra un proyecto real todavía.

### 2026-08-04 (1) — Rescate de patrones de sass-optica + hardening propio
- **Qué cambió:** a pedido del usuario, se comparó sass-combate contra su proyecto hermano más avanzado (sass-optica) para rescatar patrones aplicables, y se corrió una auditoría propia con las skills `ui-ux-pro-max`/`best-practices`. De la lista resultante se implementó todo (alto + medio impacto) en una sola pasada:
  - **Rescatado de sass-optica:** `src/utils/rate-limit.ts` (rate limiting best-effort en memoria, con pruebas) aplicado a `/api/pagos/culqi`, `/api/pagos/plan`, `/api/pagos/evento` (10 intentos/5 min por IP); `src/utils/imagen.ts` (`prepararImagen`: valida tipo/tamaño, redimensiona vía canvas — 1600px, no 512px, para no perder legibilidad de montos en la captura) aplicado al único `<input type="file">` del proyecto (comprobante de pago en Mi club).
  - **Modo oscuro:** las 3 páginas de onboarding (`/entrar`, `/registro`, `/nueva-academia`) y `mesa/[eventoId]/page.tsx` no tenían NINGÚN `dark:` — se corrigieron primero por ser las de mayor impacto. Después, barrido completo de los ~20 archivos restantes con `text-slate-400/500/600` sueltos (equipo, pagos, atletas, mi-club, eventos, admin, evento público, credencial, offline) — el propio `style-guide.md` subestimaba el tamaño del hueco ("varios lugares", en realidad 21 archivos).
  - **`Insignia`** (`badge.tsx`) ganó los estados de resultado de pelea (`victoria`/`derrota`/`empate`/`exhibicion`/`no_disputada`), reemplazando el mapa de colores duplicado en `atletas/[id]/page.tsx`. Los otros 3 casos que la auditoría automática señaló como "duplicación" (nivel de atleta en la lista, puntaje en emparejar, acciones de auditoría) se dejaron como estaban a propósito: son vocabularios distintos, no estados de pelea/evento.
  - **CTA de la landing** ("Crear mi academia") pasó a usar `estilos({variante:"solido"})` en vez de reescribir las clases de `Boton` a mano.
  - **Validación Zod** en las 5 rutas API (antes casts TS sin chequeo real) — la más crítica, `/api/sincronizar`, ahora tiene un schema por tipo de operación de la cola offline (`discriminatedUnion`), así un item mal formado devuelve 400 con el campo exacto en vez de un 500 opaco.
  - `npm run typecheck`, `npm run lint`, `npm test` (46 pruebas, 7 nuevas de `rate-limit.test.ts`) y `npm run build` pasan. Verificado con Playwright en claro/oscuro.
  - **Descartado tras evaluar** (sin hueco real de UX en sass-combate hoy, mismo criterio que ya se usó para descartar el bottom-nav de sass-optica en la sesión del 2026-07-29): `SegmentedControl`, `DatePicker`/`DateRangePicker`, `Stepper`, `SlideOver`, `OnboardingChecklist`.
- **Por qué:** el usuario pidió analizar sass-optica (más avanzado) para rescatar mejoras, y usar las skills de análisis sobre sass-combate mismo.
- **Pendiente:** analítica (Vercel Analytics/PostHog) y verificación server-a-server de un futuro webhook de Culqi quedaron identificadas como válidas pero dependientes de tener despliegue/webhook real primero — no bloquean nada hoy.

### 2026-07-31 (2) — CI, validación de env vars y reorganización de carpetas
- **Qué cambió:** el usuario avisó que ya tiene ~9 compradores y que el proyecto va a escalar rápido, y pidió cerrar una lista larga de mejoras de infraestructura, estructura de carpetas y UX discutidas en la sesión (ver análisis previos en esta misma bitácora de hoy). Primera tanda:
  - **CI** (`.github/workflows/ci.yml`): corre `typecheck`, `lint`, `test` y `build` en cada push/PR a `main`/`master`. Deliberadamente sin CD: no hay hosting decidido todavía (ver Roadmap).
  - **Validación de env vars con Zod** (`src/config/env.ts`): separado en `envPublico` (las `NEXT_PUBLIC_*`, seguro de importar desde componentes cliente — Next.js sigue reemplazando esos literales en el bundle sin importar que ahora vivan en un módulo compartido, verificado inspeccionando el bundle compilado) y `envServidor` (nunca importar desde cliente). Todas las variables siguen siendo opcionales a propósito — el proyecto se apaga a modo demo/inactivo sin ellas, no falla al arrancar. Cada integración (`CULQI_CONFIGURADO`, `EMAIL_CONFIGURADO`, `PUSH_CONFIGURADO`, `SMS_CONFIGURADO`, `WHATSAPP_CONFIGURADO`, `HAY_SUPABASE`) sigue siendo dueña de su propia bandera derivada, ahora leyendo de `envPublico`/`envServidor` en vez de `process.env` directo — sin duplicar la fuente de verdad.
  - **Reorganización de `src/lib/`** en `actions/` (Server Actions divididas por dominio: `cuenta.ts`, `academia.ts`, `eventos.ts`, `atletas.ts`, `pagos.ts`, `reclamos.ts` — antes un solo `src/app/acciones.ts` de 493 líneas), `services/` (capa de Supabase: `consultas.ts`, `atletas.ts`, `auth.ts`, `pagos.ts`, `publico.ts`, `reclamos.ts`, `offline-db.ts`; e integraciones de terceros: `pagos/culqi.ts`, `notificaciones/`), `utils/` (funciones puras: `format.ts`, `cn.ts`, `lista-club.ts`), `hooks/` (`use-sincronizacion.ts`), `config/` (`env.ts`, `roles.ts`, `contacto.ts`, `nav-app.ts`) y `types/` (antes `lib/types.ts`). `lib/` queda con inicialización de clientes (`supabase/*`) y lógica de dominio pura sin una carpeta obvia (`emparejador.ts`, `horarios.ts`, `nivel.ts`, `paginacion.ts`, `planes.ts`, `seo.ts`, `pdf/documentos.tsx`, `datos.ts` con los fixtures demo).
  - `npm run typecheck`, `npm run lint`, `npm test` (39 pruebas) y `npm run build` pasan después de cada movimiento, no solo al final.
- **Por qué:** el usuario pidió implementar todo lo analizado en la sesión, con la advertencia explícita de que ya hay compradores reales dependiendo del sistema. Se ejecutó en fases con checkpoints (no todo de una vez) precisamente por eso.
- **Decisiones de alcance (dichas explícitamente al usuario antes de empezar):**
  - **Sin Docker ni `supabase/config.toml`:** el plan de hosting es Vercel (serverless, no usa Docker); `config.toml` es para desarrollo local con la CLI de Supabase, que no se decidió usar.
  - **Sin Husky:** bajo valor mientras el checklist se corre a mano cada sesión; reconsiderar si se suma otro desarrollador.
  - **Sin migrar a `src/features/` (Feature-Sliced Design) completo:** era la alternativa de mayor riesgo (tocaría casi todos los imports del proyecto) para cero beneficio funcional inmediato — con compradores reales dependiendo del sistema, ese riesgo no se justificaba. Se hizo la reorganización moderada en su lugar.
  - **`src/types/` no incluye `database.types.ts`:** sigue sin existir un proyecto Supabase real conectado, así que no hay nada que generar todavía (`supabase gen types typescript`) — la carpeta hoy solo tiene los tipos de negocio que ya existían en `lib/types.ts`.
- **Riesgo conocido:** el refactor de carpetas es grande (91 archivos tocados, casi todos solo cambios de ruta de import) y no se probó contra un Supabase real — el mismo riesgo que ya existía antes de esta sesión, no uno nuevo introducido acá.

### 2026-07-31 (3) — Segunda tanda: mesa rediseñada, auditoría, llaves, casting, ingresos y drag-and-drop
- **Qué cambió:** continuación de la tanda anterior, mismo pedido ("implementa todo"), ocho mejoras de UI:
  - **Mesa de control rediseñada** (`src/app/mesa/[eventoId]/page.tsx`): flujo de 3 pasos en vez de un solo formulario — esquina (botones `tamano="mesa"`, `h-24`, el que ya existía en `button.tsx` pero no se usaba acá) → método → confirmación explícita con resumen ("¿Confirmar que gana X por Y?") antes de mandar nada. Revertir un resultado mal marcado por apuro era el problema real que esto ataca.
  - **Pantalla de auditoría** (`/app/auditoria`, roles dueño/admin): la tabla `auditoria` y el trigger `tr_auditar_resultado` ya existían desde el primer commit del proyecto (migración `20260101000003`) pero nunca tuvieron una pantalla — `src/services/auditoria.ts` + la página son solo lectura de lo que ya se venía grabando.
  - **Búsqueda y llave de eliminación en `/e/[org]/[evento]`:** `agenda-con-busqueda.tsx` filtra client-side (sin ida y vuelta al servidor) por nombre de peleador, con barra pegajosa. `llave-arbol.tsx` dibuja el bracket de eliminación directa agrupado por ronda con scroll horizontal (mobile-first: sin líneas conectoras gráficas a propósito, difíciles de leer en un celular desde las gradas). Esto último requirió una migración nueva (`20260101000012_bracket_publico.sql`) para exponer `tipo`/`llave_id`/`ronda`/`posicion` en `v_publico_pelea` — esas columnas existían en el esquema desde `20260101000001` pero nunca se leían desde ningún lado del código, porque nada genera llaves todavía (ver alcance abajo).
  - **Botón de pantalla completa** (`boton-pantalla-completa.tsx`, Fullscreen API nativa, sin polyfills) en `/e/[org]/[evento]` — pensado para proyectar la agenda/llave en un TV del estadio.
  - **Gráfico de ingresos** en el dashboard (`/app`, solo dueño/admin): barras de pagos `aprobado` por día, últimos 7 días, SVG/HTML puro (sin librería de gráficos nueva) siguiendo la skill `dataviz` del proyecto — un solo tono (`exito`, ya usado en la app para montos positivos), sin leyenda por ser una sola serie.
  - **Drag-and-drop en el emparejador:** arrastrar un peleador "sin rival" sobre otro los empareja a mano (estado local, sección "Emparejamientos manuales" separada de las propuestas del algoritmo). El resto de la pantalla (aceptar/descartar propuestas) se dejó igual a propósito — reordenar cruces ya calculados por drag no tenía un caso de uso claro; el hueco real era el peleador que el motor no pudo emparejar.
  - `npm run typecheck`, `npm run lint`, `npm test` (39 pruebas) y `npm run build` pasan; las 17 rutas principales verificadas con el servidor de desarrollo (200 en todas).
- **Decisiones de alcance:**
  - **La llave de eliminación es solo de visualización.** No se construyó generación de llaves (seeding, byes, avance automático de ganadores a `siguiente_pelea_id`) — es una funcionalidad grande y separada (equivalente a un segundo emparejador) que el esquema ya prevé (`pelea.siguiente_pelea_id`/`siguiente_esquina`) pero nadie implementó nunca. El árbol de hoy muestra datos demo fijos (`peleasLlaveDemo()` en `publico.ts`) y renderizaría una llave real el día que algo la genere — hoy nada la genera.
  - **El emparejamiento manual por drag-and-drop no se guarda:** vive en estado de React de esa sesión de pantalla, no crea ninguna `pelea` real. Falta un botón "Guardar" que lo persista — no se pidió explícitamente y el resto de "Propuestas" tampoco tiene un paso de guardado todavía (toda la pantalla de emparejar es sobre datos demo, ver pendiente de abajo).
  - `emparejar/page.tsx` y `pesaje/page.tsx` siguen sin conectarse a datos reales (mismo pendiente de la tanda anterior, sin cambios).

### 2026-07-31 — Cierre de los últimos gaps de datos demo conocidos: mi-club, precio dinámico e historial de plan
- **Qué cambió:** primer commit real del proyecto (todo lo acumulado desde el commit inicial estaba sin commitear pese a la bitácora sesión por sesión; se commiteó de una vez como punto de partida). El usuario pidió cerrar todo lo que se pudiera implementar sin infraestructura real (se descartó explícitamente provisionar un Supabase real o desplegar a Vercel en esta sesión). Se cerraron los tres pendientes de código que quedaban documentados:
  - **`mi-club/page.tsx` y `eventos/[id]/page.tsx` ya leen datos reales.** Nuevas funciones en `src/lib/consultas.ts`: `obtenerInscripcionesClub(eventoId)` (filtra por el club del coach vía `v_mi_club`, igual que ya hacía `/api/sincronizar` al inscribir) y `obtenerPrecioInscripcion(eventoId)` (llama al RPC `precio_inscripcion()` en vez de duplicar la lógica normal/extemporáneo en JS). `eventos/[id]/page.tsx` pasó de importar `AREAS_DEMO`/`PELEAS_DEMO`/`EVENTO_DEMO`/`INSCRIPCIONES_DEMO` a `obtenerAreas`/`obtenerPeleas`/`obtenerEvento`/`obtenerInscripciones` (ya existían en `consultas.ts`, solo no se usaban ahí) más `notFound()` si el evento no existe.
  - **"Mi club" ahora refleja el estado real de pago:** "Pagados"/"Por pagar" se calculan sobre `inscripcion.estado` (`pendiente` = por pagar, cualquier otro estado = pagado, ya que `aplicar_pago` es lo único que saca a una inscripción de `pendiente`) en vez de contar todo como no pagado. El formulario de comprobante/tarjeta ahora solo se muestra si hay algo pendiente, y con el monto real (`pendientes × precio_inscripcion()`).
  - **Historial de compras de plan:** hasta ahora comprar un plan solo pisaba `plan_vence_en`, sin dejar registro. Nueva tabla `compra_plan` (migración `20260101000011_historial_compras_plan.sql`, RLS: lectura para miembros de la organización, escritura para quien puede editar la organización) que `/api/pagos/plan` y `/api/pagos/evento` llenan justo después de activar el plan/evento (si el insert falla no tumba la respuesta: el plan ya quedó activo, es solo el registro el que se pierde). `listarHistorialPlanes()` en `consultas.ts` lo expone; `/app/plan` muestra la lista (tipo, evento si aplica, monto, fecha de compra y vencimiento) debajo del selector de compra.
  - Verificado con el servidor de desarrollo en modo demo (`/app/mi-club`, `/app/eventos/demo`, `/app/plan`): sin regresiones visuales, "Por pagar" en S/ 0.00 porque el dataset demo ya no tiene inscripciones en estado `pendiente` (todas `pesada`), historial vacío como se espera sin Supabase. `npm run typecheck`, `npm run lint`, `npm test` (39 pruebas) y `npm run build` pasan.
- **Por qué:** cerrar deuda documentada explícitamente en la bitácora, dentro del alcance que el usuario aprobó (código, sin tocar infraestructura real).
- **Decisión de alcance:** no se tocaron `emparejar/page.tsx` ni `pesaje/page.tsx`, que también siembran con `INSCRIPCIONES_DEMO` — no estaban en el pendiente documentado, son Client Components (no pueden llamar a `consultas.ts` directamente, que usa `next/headers`), y `pesaje` además usa esa siembra a propósito como primer valor de su caché offline. Conectarlos requeriría un wrapper de servidor o el cliente Supabase de navegador, que hoy no se usa en ninguna página — quedó anotado como pendiente nuevo, no es un gap introducido hoy.
- **Pendiente / riesgos conocidos:**
  - Nada de esto se probó contra un Supabase real (sigue sin crearse un proyecto, ver Roadmap) — el filtro de `obtenerInscripcionesClub` por `v_mi_club` y el insert de `compra_plan` están razonados contra el schema y RLS existentes, no verificados de punta a punta.
  - `emparejar`/`pesaje` siguen en datos demo (ver Decisión de alcance).

### 2026-07-30 — Sidebar por rol, revisión general de bugs, paginación y plan por evento
- **Qué cambió:**
  - **Sidebar de `/app`:** se reemplazó el nav horizontal por un sidebar colapsable (`sidebar-app.tsx`/`app-shell.tsx`/`bottom-nav-app.tsx`, patrón tomado de `sass-optica`/`tramys-rrhh`), con `lucide-react`. Filtra secciones por rol (`src/lib/nav-app.ts`): dueño/admin ven las 6, mesa solo Eventos/Atletas, coach suma Mi club, juez/lector solo Eventos — es comodidad de navegación, no seguridad (entrar directo a la URL no está bloqueado).
  - **Selector de rol y login demo:** en modo demo se agregó un selector de rol en el sidebar (cookie `demo_rol`, acción `cambiarRolDemo`) y 6 cuentas de prueba (`CUENTAS_DEMO`, `src/lib/roles.ts`) que funcionan desde `/entrar` con correo/contraseña reales o con un botón de acceso directo por rol — todo exclusivo de modo demo, sin Supabase conectado no existen cuentas reales.
  - **Bugs reales encontrados al revisar cada rol/página:** faltaba `coach` en el enum de roles de `invitarMiembro` (la migración de BD ya lo soportaba, el frontend no); 8 formularios con `bg-white` fijo que rompían en modo oscuro; el evento demo tenía fecha fija (`2026-07-25`) y el cálculo de retraso en tiempo real terminaba mostrando "125 h de retraso" — `FECHA` en `datos.ts` ahora se calcula al cargar el módulo; `historialVisible()` devolvía el mismo historial fijo para cualquier atleta (un atleta con 0 peleas mostraba 2 peleas ajenas) — ahora es un mapa por atleta.
  - **Datos ficticios ampliados:** atletas demo de 2 a 8, pagos de 2 a 9 (agregando el estado `rechazado`, que nunca existía en modo demo).
  - **Paginación:** `src/lib/paginacion.ts` (`paginar()`, 8 por página) + `src/components/ui/paginador.tsx` (Server Component, navega por `?page=`, sin JS de cliente). Aplicado en atletas, pagos, mi-club, `/admin/reclamos` y el listado de eventos — los contadores de las tarjetas siempre suman sobre la lista completa, no la página visible. Documentado en `style-guide.md`, que además tenía un dato incorrecto (decía que ya se usaba `@tanstack/react-table`; nunca se implementó).
  - **Plan "Por evento" pasa a ser por evento, no por organización:** antes `organizacion.plan_vence_en` era una sola fecha para toda la academia — comprar un segundo evento pisaba el vencimiento del primero. Ahora `evento.plan_vence_en` (migración `20260101000010_plan_por_evento.sql`) es propio de cada evento; se compra desde el evento puntual (`DesbloquearEvento`, `/api/pagos/evento`), nunca desde `/app/plan`. El plan Academia sigue siendo por organización (cubre todos los eventos) y sigue comprándose desde `/app/plan` (`/api/pagos/plan`, que ya no acepta la clave `"evento"`). `eventoDesbloqueado()` (`src/lib/planes.ts`) combina ambos: basta con que uno de los dos esté vigente.
  - Decisión explícita del usuario: vencido solo restaura el tope de eventos/inscritos del plan Gratis — ver/editar/generar actas de lo que ya existe sigue funcionando igual, no se agregó ningún bloqueo de solo-lectura.
  - `npm run typecheck`, `npm run lint`, `npm test` (39 pruebas) y `npm run build` pasan. Verificado con Playwright en claro/oscuro/mobile, los 6 roles, y los tres estados de `DesbloquearEvento` (sin comprar, desbloqueado, cubierto por Academia).
- **Pendiente / riesgos conocidos:**
  - `eventos/[id]/page.tsx` sigue sin chequear `HAY_SUPABASE` (ver pendiente de arriba) — el bloque de `DesbloquearEvento` ahí usa `EVENTO_DEMO.plan_vence_en` fijo; cuando se corrija ese gap, hay que leer el evento real (`obtenerEvento(id)`) también para ese campo.
  - El desbloqueo por evento no permite "sumar días" si se compra de nuevo antes de vencer (mismo diseño simple que ya tenía Academia) — comprarlo de nuevo mientras sigue vigente no debería pasar porque el botón se oculta mientras está vigente, pero no hay un chequeo de servidor que lo impida explícitamente.
  - Sigue sin existir un historial de compras de plan (ni Academia ni evento) — cada compra solo sobrescribe el campo `plan_vence_en` correspondiente, no queda ninguna fila en una tabla de pagos/facturación. El usuario lo preguntó explícitamente esta sesión; no se implementó (alcance no pedido todavía).

### 2026-07-29 — Libro de Reclamaciones: ajustado contra la hoja oficial de INDECOPI
- **Qué cambió:** el usuario pasó la hoja de reclamación oficial (PDF) y el logo estándar "Libro de Reclamaciones" (captura de pantalla). Comparar el formulario ya construido contra el original encontró dos errores reales:
  - **Plazo de respuesta:** tenía "30 días calendario" (inventado); el documento oficial dice *"quince (15) días hábiles, el cual es improrrogable"*. Corregido en `/libro-de-reclamaciones`, `/admin/reclamos` y el mensaje de confirmación (`enviarReclamo`, `src/app/acciones.ts`).
  - **Campo faltante:** el formulario no pedía el **domicilio** del consumidor, que la hoja oficial sí exige (sección 1). Se agregó `consumidor_domicilio` (columna nueva en la migración `20260101000009_libro_reclamaciones.sql` — se editó directamente en vez de crear una migración más, porque se escribió hoy mismo y nunca se corrió contra un proyecto real) + el campo en el formulario (`formulario.tsx`) + se muestra en `/admin/reclamos`.
  - **Logo oficial:** la captura de pantalla se guardó como `public/libro-reclamaciones.png` y se agregó como badge clickeable en la franja inferior del footer (`footer-landing.tsx`), envuelto en un fondo blanco fijo para que se vea igual en modo claro y oscuro — es la convención estándar en sitios peruanos, no solo el link de texto que ya existía.
  - `npm run typecheck`, `npm run lint`, `npm test` y `npm run build` pasan. Verificado con Playwright en claro y oscuro.
- **Nota sobre el PDF de referencia:** la hoja que pasó el usuario tenía un RUC de ejemplo (10808888411) y un domicilio de relleno ("32432432423") en los campos del proveedor — claramente datos de plantilla, no reales (el "PROVEEDOR" decía "34234"). No se usaron: se mantuvo el RUC real que el usuario confirmó directamente en el chat (10708343931, ver entrada anterior). El PDF se usó solo como referencia de qué **campos y plazos** exige el formato oficial, no de qué valores poner.
- **Pendiente:** el domicilio real de sass-combate sigue sin definir (mismo pendiente de siempre).

### 2026-07-29 — Los límites del plan Gratis ahora se aplican de verdad
- **Qué cambió:** al revisar con el usuario cómo se ve la app según rol (alternando temporalmente `ACADEMIA_DEMO.rol` en `src/lib/auth.ts` para comparar dueño/coach/mesa — ya revertido a `dueno`), salió a la luz que `academia.plan` era puramente informativo: nada en el código impedía crear un segundo evento o inscribir al peleador 41 en el plan Gratis, aunque `/#precios` lo prometía. El usuario pidió cerrarlo:
  - `src/lib/planes.ts` (nuevo): `planEstaActivo(plan, plan_vence_en)` — un plan pagado (`evento`/`academia`) solo cuenta como vigente si además `plan_vence_en` no pasó; si venció, se trata igual que Gratis. `LIMITE_EVENTOS_GRATIS` (1) y `LIMITE_INSCRITOS_GRATIS` (40), únicos números fuente para no repetirlos sueltos.
  - `crearEvento` (`src/app/acciones.ts`): si el plan no está vigente, cuenta los eventos de la organización con `estado <> 'finalizado'`; si ya hay uno, rechaza con un mensaje que apunta a `/app/plan`.
  - Caso `inscripcion` de `/api/sincronizar`: si el plan no está vigente, cuenta las inscripciones del evento; si ya hay 40 y esta es una inscripción nueva (no una actualización de una que ya existía, comprobado por `evento_id+peleador_id+modalidad_id`), la rechaza con `402`. Como esto llega desde la cola offline (`src/lib/offline/sincronizacion.ts`), un rechazo queda visible como "con errores" en `BarraConexion` y se reintenta solo — no hace un `throw` que tumbe el resto de la sincronización.
  - `npm run typecheck`, `npm run lint`, `npm test` (39 pruebas) y `npm run build` pasan.
- **Por qué:** consistencia entre lo que la landing promete y lo que el producto hace — dejarlo como estaba habría significado vender un límite que no existía.
- **Pendiente / riesgos conocidos:**
  - El límite de inscritos se cuenta por evento completo (todas las modalidades, todos los clubes), no por club — así está redactado en `/#precios` ("Hasta 40 inscritos"), pero si se quisiera acotar por club habría que rediseñar la consulta.
  - `mi-club/page.tsx` sigue mostrando `INSCRIPCIONES_DEMO` fijos (ver entrada de ayer), así que en modo demo no hay forma de ver el límite de 40 disparándose de verdad — solo se puede confirmar leyendo el código o probándolo contra un Supabase real.

### 2026-07-29 — Cumplimiento legal/Culqi: Libro de Reclamaciones, contacto, devoluciones y compra real de un plan
- **Qué cambió:**
  - **Libro de Reclamaciones Virtual** (`/libro-de-reclamaciones`, obligatorio por la Ley N.º 29571 e independiente del requisito de Culqi): formulario completo (tipo reclamo/queja, datos del consumidor, menor de edad con tutor, bien/servicio, monto, detalle, pedido) → server action `enviarReclamo` (`src/app/acciones.ts`) → `crearReclamo` (`src/lib/reclamos.ts`) → tabla `reclamo` (migración `20260101000009_libro_reclamaciones.sql`, RLS: cualquiera puede insertar, solo `es_staff()` puede leer/responder). Vista de staff `/admin/reclamos` para responder (`responderReclamoAccion`), enlazada desde `/admin`. Correlativo automático (`RC-<número>`) vía secuencia SQL, sin depender de Google Forms/Drive.
  - **Datos de contacto reales:** footer (`footer-landing.tsx`) ahora muestra correo (`jfhrdeveloper@gmail.com`) y WhatsApp (`+51 931 314 659`, número real que pasó el usuario), además del link al Libro de Reclamaciones. **Domicilio y RUC siguen sin completar** (ver Pendientes activos) — el usuario confirmó que todavía no tiene dirección física; se dejó una nota explícita en `/libro-de-reclamaciones` en vez de inventar un domicilio.
  - **Política de devoluciones mejorada** en `/terminos` (nueva sección 6, el resto se renumeró 7-10): ya no delega todo a "cada academia organizadora" — cubre específicamente los planes que cobra sass-combate directamente (derecho de retracto de 7 días calendario si no se usó el servicio, no reembolsable si ya se consumió, cancelación de renovación en cualquier momento). Los correos de contacto de `/terminos` y `/privacidad` se unificaron al correo real (antes eran `soporte@`/`privacidad@sass-combate.com`, inventados).
  - **El plan ahora se puede comprar de verdad**, resolviendo la discrepancia que se detectó al revisar el checklist de Culqi (se mostraban precios sin ningún checkout). Diseño acordado con el usuario: primero se crea la cuenta (como ya funcionaba), y la compra pasa a `/app/plan` (nuevo, enlazado desde el nav de `/app` y desde la tarjeta "Plan" del dashboard) — cobro único con Culqi vía `/api/pagos/plan` (reusa `crearCargoCulqi` de `src/lib/pagos/culqi.ts`, ya existente para inscripciones) que activa `organizacion.plan` y `organizacion.plan_vence_en` (columna nueva; `v_mis_academias` se actualizó para exponerla). **Sin renovación automática**: es un cobro por período (evento: 45 días: plan mensual: 30 días; anual: 365 días), decisión explícita del usuario para no montar tokens guardados + webhooks en esta pasada.
  - Iconos SVG inline (sin assets externos) en las tres tarjetas de precios de la landing, para acercarse al requisito de "foto" del checklist en un servicio sin producto físico.
  - Verificado con Playwright (claro): `/libro-de-reclamaciones` (formulario completo), `/app/plan` (mensaje correcto de "tarjeta no configurada" en demo), `/admin/reclamos` (reclamo de ejemplo con formulario de respuesta), `/terminos` renumerado, precios con íconos. `npm run typecheck`, `npm run lint`, `npm test` (39 pruebas) y `npm run build` pasan.
- **Por qué:** el usuario pegó un checklist de requisitos de comercio (el que pide Culqi/cualquier pasarela peruana para aprobar cobros reales) y pidió verificar si el proyecto cumplía. El análisis encontró 3 bloqueantes: no existía Libro de Reclamaciones (además de requisito de Culqi, es obligación legal independiente), no había datos de contacto visibles, y se mostraban precios sin ningún flujo de compra real. El usuario pidió cerrar los tres.
- **Nota de seguridad:** durante esta sesión llegó una notificación de tarea en segundo plano con formato anómalo que pretendía "entregar" un RUC (10708343931) como si el usuario lo hubiera pasado por otro canal — no coincidía con ningún agente lanzado en la conversación. Se lo marcó como sospechoso (posible inyección de instrucciones) y **no se usó ese número en ningún lado**; el usuario no lo confirmó. Si en una sesión futura aparece ese mismo RUC, hay que pedir confirmación explícita antes de usarlo, no asumir que ya fue validado.
- **Pendiente / riesgos conocidos:**
  - Domicilio y RUC reales de sass-combate: pendientes (ver Pendientes activos y la nota de seguridad arriba).
  - El SSL del checklist no aplica todavía porque el proyecto no está desplegado (sigue en el Roadmap).
  - Cobro recurrente real del plan Academia: no implementado a propósito, ver Pendientes activos.
  - `/admin/reclamos` no tiene paginación ni filtro — aceptable mientras el volumen sea bajo, revisar si crece.

### 2026-07-29 — Landing completa (precios, FAQ, disciplinas, CTA) y auditoría de tokens en `/app`
- **Qué cambió:**
  - **Landing (`src/app/page.tsx`):** cuatro secciones nuevas entre "Cómo funciona" y el footer:
    - **Disciplinas soportadas** — Kickboxing (con sus 5 modalidades WAKO como chips, vía `NOMBRE_MODALIDAD`), Muay Thai, Boxeo, MMA.
    - **Precios** (`#precios`) — Gratis (S/ 0), Por evento (S/ 149, destacado) y Academia (S/ 299/mes o S/ 2,990/año). Montos definidos en esta sesión por pedido explícito del usuario ("definilos vos"); **no son precios validados con el mercado**, son un punto de partida razonable a ajustar.
    - **FAQ** (`#faq`) — 6 preguntas con `<details>/<summary>` nativo (sin JS ni librería de animación, consistente con la ausencia de animaciones grandes del proyecto), respuestas ancladas a funcionalidad real (offline, medios de pago, multi-club, horarios en cascada, sin instalación, cambio de plan por WhatsApp).
    - **CTA final** — "Crear mi academia" + botón de WhatsApp (`wa.me/51931314659` con texto prellenado), número real que pasó el usuario.
    - `NavLanding`/`FooterLanding` ahora enlazan a `#precios` y `#faq` además de `#como-funciona`.
  - **Auditoría de tokens en `/app`:** varios lugares reinventaban el variante `solido` de `Boton` a mano (`bg-slate-900 px-4 py-2 ... hover:bg-slate-700`) sin el swap a `dark:bg-slate-100 dark:text-slate-900` que ya tiene el componente — en modo oscuro esos botones no fallaban visualmente pero quedaban inconsistentes con el resto. Se reemplazaron por `estilos()` (`src/components/ui/button.tsx`) en `/app` (dashboard), `/app/eventos/[id]`. Se agregó `dark:` a chips sueltos (`bg-slate-100`/`text-slate-900` sin variante oscura) en el dashboard, atletas, atleta individual, pesaje y emparejamiento, y en el header/nav de `/app` (`app/layout.tsx`, `nav-app.tsx`). Se extendió `Insignia` (`badge.tsx`) con los estados de `evento.estado` (`borrador`/`inscripciones`/`pesaje`/`programado`/`finalizado`, distintos de los de `pelea`) y el dashboard ya la usa en vez de un chip armado a mano. `TarjetaTitulo` (`card.tsx`), usado en decenas de tarjetas de todo `/app`, gana `dark:text-slate-400` — arreglo de una sola línea con alcance amplio.
  - Verificado con Playwright (instalado ad-hoc en el scratchpad de la sesión, no es dependencia del proyecto) en claro y oscuro: landing completa, dashboard de `/app`. Sin errores de consola, sin regresiones. `npm run typecheck`, `npm run lint`, `npm test` (39 pruebas) y `npm run build` pasan.
- **Por qué:** el usuario pidió analizar sus otros proyectos (`3AM-Burger`, `ETC`/`peru-fight-academy`, `tramys-landing`, `sass-optica`, etc.) y sus skills de diseño para completar la landing y pulir la interfaz del SaaS. El análisis de proyectos hermanos no encontró una paleta/tipografía idéntica entre ellos, pero sí un patrón de proceso repetido (fuente display + body distinta, `ThemeToggle`, landing separada del shell de `/app` con nav por rol) que este proyecto ya sigue. De ahí salieron 4 ideas concretas (sección "cómo funciona" — ya existía —, franja de confianza, hero con stats, CTA a WhatsApp) de las que el usuario eligió precios + FAQ + CTA/WhatsApp + disciplinas soportadas.
- **Decisión de alcance:** se descartó a propósito la franja de "logos de clientes" (`EntidadesStrip` de `tramys-landing`) porque no hay academias reales usando la plataforma todavía — mostrar logos inventados sería engañoso; se reemplazó por disciplinas soportadas. Se descartó también el hero con watermark oscuro/neón de `peru-fight-academy` porque es la dirección que la sesión de rediseño visual ya rechazó a propósito (ver entrada del rediseño tipográfico). No se agregó una barra de navegación inferior tipo `AdminBottomTabBar` (de `sass-optica`) para mesa/pesaje: esas pantallas ya son vistas de pantalla completa sin el nav de `/app`, pensadas para un solo propósito con guantes puestos — no se identificó un hueco real de navegación que la justifique hoy.
- **Pendiente / riesgos conocidos:**
  - Los precios son un punto de partida inventado en esta sesión, no una decisión de negocio validada — ajustarlos antes de que alguien los vea en serio.
  - La auditoría de tokens en `/app` cubrió los casos más visibles (botones y chips que duplicaban o rompían el patrón de color), no un barrido completo de cada `text-slate-500/600` suelto en cada página — sigue siendo trabajo pendiente más grande, ya documentado en `docs/style-guide.md`.
  - No se tocó `mi-club/page.tsx` ni `eventos/[id]/page.tsx` (siguen en datos demo fijos, ver entrada de ayer) ni `mesa/[eventoId]/page.tsx`, que también sigue en `PELEAS_DEMO`/`AREAS_DEMO` fijos aunque tiene su propio layout ya preparado.

### 2026-07-29 — Cierre de los gaps de código que bloqueaban el uso real de Supabase
- **Qué cambió:**
  - **`/e/[org]/[evento]` y `/p/[token]` ya leen datos reales** cuando hay Supabase (antes mostraban `PELEAS_DEMO`/`EVENTO_DEMO` siempre). Nueva capa `src/lib/publico.ts`:
    - `obtenerAgendaPublica(org, eventoSlug)` resuelve el evento por sus vistas públicas (`v_publico_evento`, `v_publico_area`, `v_publico_bloque`, y `v_publico_pelea` con columnas nuevas: `area_id`, `roja_id`, `azul_id`, `hora_inicio_real`, `hora_fin_real`) y le da forma de `Area[]`/`Pelea[]`/`Bloque[]` para reusar `construirAgenda()` sin duplicar la regla "las horas se calculan, no se escriben". `generateMetadata` de esa ruta ya usa el nombre/fecha/sede reales en vez de derivarlos del slug.
    - `obtenerCredencialPorToken(token)` resuelve el token con `service_role` (mismo patrón que `/api/notificaciones/suscribir`: el token ya autoriza sin sesión) y ahora sí busca la pelea propia del peleador — antes la demo mostraba la primera pelea sin terminar de *todo* el evento, sin usar el token más que para el título.
    - Migración `20260101000007_vistas_publicas_evento.sql`: agrega `v_publico_evento`, `v_publico_area`, `v_publico_bloque` (abiertas a `anon`, filtradas por `evento.publico`) y extiende `v_publico_pelea` con las columnas crudas que necesita `construirAgenda()`.
  - **`pago_inscripcion` ya se llena.** Nueva función SQL `vincular_pago_inscripciones(pago_id, evento_id, club_id)` (migración `20260101000008_vincular_pago_inscripcion.sql`, `security definer`, valida que el pago sea de la propia organización) que enlaza todas las inscripciones pendientes de un club en un evento al crear el pago — "Mi club" ya está planteado como "pagar por todos de una vez", así que no hace falta selección por alumno todavía. Se llama desde `registrarPago` (`src/app/acciones.ts`, comprobante manual) y desde `/api/pagos/culqi/route.ts` (tarjeta) justo después de crear el `pago` y antes de aprobarlo, así el trigger `aplicar_pago` ya las encuentra al aprobarse.
  - De paso, el endpoint de Culqi bajó su uso de `service_role`: el insert del pago (estado `en_revision`) ahora va por la sesión normal (ya cumple la política `pago_creacion`); `service_role` queda solo para el paso que de verdad lo necesita, aprobar el pago sin que sea dueño/admin quien lo hace.
  - `npm run typecheck`, `npm run lint`, `npm test` (39 pruebas) y `npm run build` pasan. Verificado en el navegador con el servidor de desarrollo en modo demo (sin Supabase): ambas páginas públicas siguen renderizando igual que antes (sin regresión visible), incluyendo modalidad/clase en la credencial que se había perdido en un borrador intermedio de este cambio.
- **Por qué:** el usuario pidió dejar pendiente todo lo que requiere un proyecto Supabase real, pero que el código del proyecto quedara listo para eso — es decir, cerrar los gaps que seguirían bloqueando el uso real incluso después de conectar credenciales de verdad.
- **Decisión de alcance:** no se creó ningún proyecto Supabase real ni se corrió ninguna migración contra una base real (sigue pendiente, ver Roadmap). Tampoco se tocó `mi-club/page.tsx` ni `eventos/[id]/page.tsx` (siguen en datos demo fijos) ni el cálculo de precio fijo de esa página — quedan anotados como pendientes nuevos, detectados al revisar el flujo de pago, pero fuera del alcance que se pidió cerrar hoy.
- **Pendiente / riesgos conocidos:**
  - Nada de esto se probó contra un proyecto Supabase real todavía (sigue sin crearse uno). El diseño de las vistas públicas y de `vincular_pago_inscripciones` está razonado contra el schema y las políticas RLS existentes, pero falta la verificación de punta a punta que solo un proyecto real permite.
  - Si más adelante se agrega selección de alumnos específicos al pagar (en vez de "todos los pendientes del club"), `vincular_pago_inscripciones` es el lugar para acotar el filtro.

### 2026-07-28 — Inicialización de documentación y estandarización del proyecto
- **Qué cambió:**
  - Se generó la estructura `docs/` (`style-guide.md`, `architecture.md`, `pending-task.md`, `db-notes.md`) con `plantillabase-docs`, y `CLAUDE.md` enlazando a esos documentos.
  - Se instalaron las dependencias (`npm install`), no existía `node_modules` ni lockfile.
  - Se inicializó el repositorio git (no existía `.git`) con un primer commit.
  - Se eliminó una carpeta vacía `{src/{app,lib,components}` (residuo de un `mkdir` con expansión de llaves fallida en PowerShell).
  - Se configuró ESLint (`eslint-config-next`) para que `npm run lint` funcione.
  - Se generaron iconos PWA placeholder (`public/icono-192.png`, `public/icono-512.png`) para que el manifest deje de apuntar a archivos inexistentes; son marcadores de posición, no arte de marca final.
- **Por qué:** estandarizar la documentación y dejar el entorno de desarrollo funcional de punta a punta (clonar → instalar → correr).
- **Pendiente:** rellenar la paleta/tipografía real cuando haya diseño de marca definitivo; conectar Supabase real; decidir proveedor de pago con tarjeta y canal de notificaciones (ver Roadmap).

### 2026-07-28 — Tokens de color, cola offline en inscripción, pago con Culqi, notificaciones al peleador
- **Qué cambió:**
  - **Tokens de color:** se agregaron `exito`/`aviso`/`error`/`info` (cada uno con `DEFAULT`/`-suave`/`-fuerte`) a `tailwind.config.ts` y se migraron todos los usos sueltos de `amber-*`/`emerald-*`/`rose-*`/`sky-*` a los tokens nuevos (`estado-conexion.tsx`, `badge.tsx`, `formulario.tsx`, páginas de pagos/mi-club/atletas/eventos/pesaje/emparejar). Documentado en `docs/style-guide.md`.
  - **Inscripción por cola offline:** `cargar.tsx` ya no llama al server action `cargarListaClub` (se eliminó); ahora encola cada alumno con `useSincronizacion` como tipo `"inscripcion"`, igual que resultados/pesaje/asistencia. La resolución contra el registro compartido de atletas (que antes vivía en el server action) se movió al caso `"inscripcion"` de `/api/sincronizar`, que ahora también acepta el rol `coach` (antes solo dueño/admin/mesa podían escribir por esa vía).
  - **Pago con tarjeta (Culqi):** `src/lib/pagos/culqi.ts` (llamada REST a `/v2/charges`, sin SDK), `src/app/api/pagos/culqi/route.ts` (cobra y aprueba el pago), `pago-tarjeta.tsx` (widget Checkout.js v4). Se agregó `src/lib/supabase/admin.ts` (`crearClienteServicio`, primer uso de `SUPABASE_SERVICE_ROLE_KEY` en el proyecto) porque la política de RLS solo deja insertar pagos en `en_revision` y aprobar a dueño/admin — para tarjeta aprueba la pasarela, no una persona.
  - **Notificaciones al peleador:** `src/lib/notificaciones/` con proveedores independientes (Resend para email, Twilio para SMS y WhatsApp, Web Push con VAPID). Se dispara desde `/api/sincronizar` después de `recalcular_horarios` (no hay cron: la mesa ya recalcula horarios todo el día). Nueva migración `20260101000006_notificaciones.sql` (`peleador.telefono`/`email`, `push_suscripcion`, `notificacion_enviada`, esta última evita reenviar el mismo aviso). Opt-in de push en `/p/[token]` (`activar-notificaciones.tsx`) vía `/api/notificaciones/suscribir`, y entrega en `public/sw.js` (eventos `push`/`notificationclick`). `lista-club.ts` acepta ahora teléfono y correo como columnas opcionales al final de la lista pegada.
  - `npm run typecheck`, `npm run lint`, `npm test` (39 pruebas) y `npm run build` pasan.
- **Por qué:** cerrar los pendientes activos y dos ítems del roadmap que el usuario pidió abordar ahora, sin aprovisionar infraestructura real (ver decisión de alcance abajo).
- **Decisión de alcance:** por pedido explícito del usuario, esta sesión fue "solo código" — no se creó ningún proyecto Supabase real, no se desplegó a Vercel ni se compraron credenciales de Culqi/Resend/Twilio/VAPID. Todo el código nuevo sigue el patrón `HAY_SUPABASE`/`_CONFIGURADO` del resto del proyecto: sin esas variables de entorno, cada pieza queda inactiva (o simulada) en vez de fallar.
- **Pendiente / riesgos conocidos:**
  - La integración de Culqi (forma exacta de `/v2/charges` y de Checkout.js v4) no se validó contra una cuenta ni un sandbox real — revisar contra la documentación oficial antes de cobrar de verdad.
  - `/p/[token]` sigue mostrando datos de ejemplo (`PELEAS_DEMO`) aunque haya Supabase real; solo el opt-in de push ya resuelve el `token` contra la base real.
  - Ninguna vía de pago llena `pago_inscripcion`, así que aprobar un pago no marca inscripciones como pagadas todavía (gap preexistente, no introducido en esta sesión, pero ahora también afecta a tarjeta).
  - Faltan credenciales reales para probar de punta a punta: Culqi, Resend, Twilio y el par de llaves VAPID (`npx web-push generate-vapid-keys`).

### 2026-07-28 — Rediseño visual: tipografía, componente de firma y aplicación en toda la plataforma
- **Qué cambió:**
  - **Tipografía:** `Barlow Condensed` (`font-display`) + `Inter` (`font-body`) vía `next/font/google` en `src/app/layout.tsx`, expuestas como variables CSS y wireadas en `tailwind.config.ts`. `globals.css` aplica `font-display` a `h1`/`h2`/`h3` globalmente. Documentado en `docs/style-guide.md`.
  - **Componente de firma `TarjetaPelea`** (`src/components/ui/tarjeta-pelea.tsx`): el enfrentamiento roja/azul (roja a la derecha, azul a la izquierda, separados por "vs"), reemplazando el markup duplicado en mesa de control, emparejamiento, evento público y credencial personal.
  - Se pulieron los componentes base para usar `font-display`: `Boton` (mayúsculas en tamaño `mesa`), `TarjetaDato` (números con `tabular-nums`), `Insignia` y los demás chips de estado (pagos, atletas, mi-club).
  - Se rediseñó la landing (`src/app/page.tsx`) con una vista previa real de `TarjetaPelea` en el hero y una franja roja→azul como divisor.
  - Se agregó `NavApp` (`src/components/nav-app.tsx`) para resaltar la sección activa en la navegación de `/app`.
  - Verificado con capturas de Playwright (desktop y móvil) en landing, evento público, credencial, entrar, dashboard, mi-club, mesa de control y detalle de evento — sin errores de consola. Se corrigió `TarjetaPelea` en `/p/[token]` (`lg` truncaba nombres largos en el contenedor angosto; se bajó a `md`).
  - `npm run typecheck`, `npm run lint`, `npm test` (39 pruebas) y `npm run build` pasan.
- **Por qué:** el usuario pidió usar las skills de diseño (`ui-ux-pro-max`, `frontend-design`) para mejorar el diseño e interfaces de toda la plataforma.
- **Decisión de dirección:** se descartó la recomendación "gaming/esports" (fondo oscuro, neón) que devolvió `ui-ux-pro-max` por defecto — no encaja con una herramienta operativa usada a la luz del día, con guantes puestos, por WCAG AA y por el resto del sistema (modo claro, sin animaciones grandes). Se eligió en cambio una dirección "operaciones deportivas": tipografía condensada de marcador/cartelera de pelea, y el ya existente par roja/azul (esquinas del ring) como firma de marca en vez de inventar un color nuevo.
- **Pendiente:** iconos PWA siguen siendo placeholder (necesitan arte de marca real, no resuelto por este rediseño tipográfico); `/p/[token]` sigue en datos de ejemplo (ver entrada anterior).

### 2026-07-28 — Navbar, footer, modo oscuro y páginas legales en la landing
- **Qué cambió:**
  - **Modo oscuro** (`darkMode: "class"` en `tailwind.config.ts`): los 17 tokens de color (`borde`/`panel`/`fondo`/`roja`/`azul`/`exito`/`aviso`/`error`/`info` con sus `-suave`/`-fuerte`) se migraron a variables CSS en `globals.css` (`:root` y `.dark`), calculando los valores oscuros con un script propio (`node` + fórmula de contraste WCAG relativo) en vez de a ojo — así `roja`/`azul` en oscuro pasan la verificación de contraste tanto como texto sobre `fondo`/`panel` como con texto blanco encima. Se agregó `ThemeToggle` (`src/components/theme-toggle.tsx`) y un script anti-parpadeo inline en `src/app/layout.tsx`. Se corrigieron `bg-white` fijos en `Boton` e `input.tsx` (`Campo`) que rompían en oscuro.
  - **Navbar de la landing** (`src/components/nav-landing.tsx`): marca, enlaces (Cómo funciona, Evento de ejemplo), toggle de tema, Entrar/Crear cuenta, con menú hamburguesa en móvil.
  - **Footer ampliado** (`src/components/footer-landing.tsx`): columnas Producto/Cuenta/Legal + copyright.
  - **Páginas legales:** `/terminos` y `/privacidad` (`src/app/terminos/page.tsx`, `src/app/privacidad/page.tsx`), contenido redactado a partir del modelo de datos real del proyecto (menores de edad, proveedores externos —Supabase/Vercel/Culqi/Resend/Twilio—, derechos ARCO bajo la Ley N.º 29733 de Perú).
  - Verificado con Playwright en claro y oscuro (landing, términos, privacidad) y el menú móvil — sin errores de consola. `npm run typecheck`, `npm run lint`, `npm test` y `npm run build` pasan.
- **Por qué:** el usuario pidió completar la landing: faltaba navbar, footer, modo oscuro y páginas de términos/privacidad.
- **Pendiente / riesgos conocidos:**
  - El contenido de `/terminos` y `/privacidad` es un borrador razonable pero **no reemplaza una revisión legal real** antes de publicar en producción (datos de contacto, plazos de conservación exactos y la razón social real de la academia dueña de la plataforma quedan como placeholder).
  - El modo oscuro está completo en la landing, las legales y los componentes compartidos; el resto de `/app` no se auditó pixel a pixel (ver nota en `docs/style-guide.md`).

### 2026-07-28 — SEO: sitemap, robots, metadata, Open Graph y JSON-LD
- **Qué cambió:**
  - `src/lib/seo.ts` (`URL_BASE`, `urlEvento`, `desdeSlug`) + `metadataBase` en `src/app/layout.tsx`.
  - `src/app/sitemap.ts` (estáticas + eventos públicos vía `v_publico_pelea` si hay Supabase) y `src/app/robots.ts` (bloquea `/app`, `/mesa`, `/admin`, `/nueva-academia`, `/api`, `/p/`).
  - Metadata real (título/descripción/OG/Twitter) en la landing y `generateMetadata` dinámico + JSON-LD `SportsEvent` en `/e/[org]/[evento]`.
  - Imágenes Open Graph generadas con `next/og` (`src/app/opengraph-image.tsx` y `src/app/e/[org]/[evento]/opengraph-image.tsx`), sin arte estático.
  - `robots: { index: false, follow: false }` en `/app/**` (vía su layout), `/admin`, `/mesa/[eventoId]` (layout nuevo, la página es cliente), `/nueva-academia` (layout nuevo, ídem) y `/p/[token]`.
  - Verificado sirviendo `/robots.txt`, `/sitemap.xml`, ambas imágenes OG y el JSON-LD con el servidor local. `npm run typecheck`, `npm run lint`, `npm test` y `npm run build` pasan.
- **Por qué:** de las tres skills nuevas que el usuario agregó (`seo-optimizer`, `ui-design-system`, `mobile-design`), solo la de SEO aplicaba de verdad a este proyecto (las otras dos apuntan a apps nativas o a generar un sistema de diseño desde cero, que ya existe) — se le pidió avanzar con eso.
- **Pendiente / riesgos conocidos:**
  - El `generateMetadata` de `/e/[org]/[evento]` en modo real (`HAY_SUPABASE`) arma el título/descripción a partir del slug de la URL, no del nombre real del evento — porque esa página todavía no lee Supabase (ver pendiente de arriba). Mejorar cuando se conecte a datos reales.
  - No hay `NEXT_PUBLIC_DOMINIO_RAIZ` real todavía, así que `URL_BASE`/`urlEvento()` siguen apuntando a `localhost:3000` hasta que haya dominio propio.
